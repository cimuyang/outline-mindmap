# 大纲思维导图（outline-mindmap）

[English](README.md) · **中文** · [插件介绍（可在 Obsidian 里直接打开的演示笔记）](插件介绍.md)

把 Markdown 笔记的标题大纲渲染成思维导图，**导图与笔记双向实时编辑**。

在导图里改一个节点，改的就是笔记里的那一行；在笔记里敲一个字，导图立刻跟着变。
两边是同一份数据的两个视图，没有第三份状态。

![](https://github.com/cimuyang/outline-mindmap/blob/main/demo.gif)

## 特色功能

1. **严格 Markdown 语法，笔记零污染**：标题和列表自动变成导图，不写 YAML、不插属性、不加隐藏注释。所有设置存在插件自己的 `data.json` 里——你的 Markdown 永远是 Markdown，拿到任何地方都干净可用。
2. **双向实时同步**：改笔记，导图跟着变；拖导图，笔记同步改。写回只替换那一行，范围外逐字节不变；一次操作一步撤销，两边永远一致。
3. **点击即定位**：单击节点，笔记滚到对应行并高亮；双击就能改字。新建、改名、拖拽之后，笔记也停在你刚动过的那个节点上，大纲与正文之间无缝切换。
4. **拖拽重排**：拖到上下缘 = 插到前后，拖到中间 = 成为子节点，拖到空白 = 变成新的根。跨过第 6 层时标题与列表自动互转，底下的正文跟着一起搬家。
5. **快捷键操作**：`Enter` 加兄弟、`Tab` 加孩子、方向键走位、`Delete` 删整棵子树、`Esc` 一律作废不写盘——双手不离键盘。
6. **好看样式**：形状、配色、字号、疏密随手调，连线可选直线 / 斜线 / 折线，布局可向右、向左或两侧均衡分列。全局与单篇两级，拖滑块实时预览，取消即还原。
7. **优雅动画**：节点移动、切换布局、展开折叠都能平滑过渡；默认关着，要不要这份顺滑由你决定。而折叠完，你刚点的那个节点永远在正中间。

## 三条不会破的底线

1. **绝不写 frontmatter、绝不写标签。** 所有设置与样式存在插件自己的 `data.json` 里，
   笔记里一个字节都不多。
2. **绝不整篇重写文件。** 每次写回都是对特定行范围的最小替换，范围外的字节逐字节不变——
   你手写的空行、缩进、行尾、换行符（LF / CRLF）原样保留。
3. **一次操作 = 一次撤销。** 所有写入都走编辑器事务，`Ctrl+Z` 一下就能退回去。

## 安装

Obsidian 社区插件市场尚未收录，先手动安装：

1. 把 `main.js`、`manifest.json`、`styles.css` 三个文件放进
   `<你的仓库>/.obsidian/plugins/outline-mindmap/`
2. 在「设置 → 第三方插件」里启用「大纲思维导图」

本仓库里带了构建好的 `main.js`，直接下载这三个文件就能用，不必先装 Node。

从源码构建：

```bash
npm install
npm run build     # 类型检查 + 打包出 main.js
```

## 使用

打开导图有三种方式：

- 左侧栏的导图图标 → 在**右侧边栏**打开
- 命令面板「打开大纲思维导图」→ 在**主页面区**新标签页打开
- 命令面板「在侧边栏打开大纲思维导图」

两种形态可以同时开着，互不干扰。导图默认跟着当前笔记走，
想让它停在某一篇上，打开设置里的「锁定当前笔记」。

### 笔记怎么变成导图

| 笔记里的写法 | 导图里的层级 |
| --- | --- |
| `# 标题` … `###### 标题` | 第 1–6 层 |
| `- 列表项`（一层缩进 = 4 个空格或一个 Tab） | 第 7 层起 |
| 标题下面的正文段落 | 不显示，但**跟着标题一起移动** |
| 代码块里的 `#` | 不是标题，不会出现在导图里 |

节点文字里的 `**粗体**`、`*斜体*`、`` `代码` ``、`[链接]()`、`==高亮==` 会照着渲染。

### 快捷键

| 按键 | 作用 |
| --- | --- |
| `↑` `↓` | 上一个 / 下一个节点：先找同级兄弟，没有了就退到外层 |
| `→`（「进入」方向） | 分支收着就展开它，已经展开就进到第一个子节点 |
| `←`（「返回」方向） | 分支展开着就收起它，已经收起（或没有子节点）就回到父节点 |
| `Enter` | 在当前节点后面新增一个兄弟节点 |
| `Tab` | 给当前节点新增一个子节点 |
| `F2` / 双击节点 | 重命名 |
| `Delete` / `Backspace` | 删掉选中的节点及其整棵子树 |
| `Ctrl/⌘ + Z` | 撤销（转发给编辑器） |
| `Esc` | 放弃当前编辑或当前拖拽，笔记一个字都不写 |
| 双击空白处 | 新建一个自由根节点 |
| `Ctrl/⌘ + 左键` | 加选 / 减选（用于一次删掉一批） |
| 拖拽节点 | 重排：拖到节点上下缘 = 插到前 / 后，拖到中间 = 成为它的子节点，拖到空白 = 变成新的根 |
| 滚轮 | 平移；按住 `Ctrl/⌘` 滚 = 以鼠标为中心缩放 |

方向键的左右是**按节点自己朝哪边生长**来解释的：分支向左时，`←` 才是「进到子节点里去」。

### 工具栏

适应画布 · 缩小 · 放大 · 布局（分支向右 / 向左 / 两侧）· 展开全部 · 折叠全部 · 样式设置。

「分支两侧」会把根节点的子分支**按子树规模**均衡分到左右两列。

## 设置

| 开关 | 默认 | 说明 |
| --- | --- | --- |
| 单击即跳转 | 开 | 单击节点时把编辑器滚到对应标题并高亮；在导图里新建 / 重命名 / 删除 / 拖拽之后，笔记也停在那个节点处。关掉后单击只选中，编辑器一律不滚 |
| 锁定当前笔记 | 关 | 导图不再跟着活动笔记切换 |
| 优雅动画 | **关** | 节点移动、切换布局、展开/折叠时的视野跟随都做平滑过渡。节点多时会明显变卡 |
| 严格换行 | 开 | 新增 / 移动节点时，相邻两个标题之间补足 3 个空行 |

样式（形状、配色、字号、横纵间距、分支样式：直线 / 斜线 / 折线）分**全局**与**单篇**两级：
单篇没设就用全局的。样式窗口开着时拖滑块会实时预览，取消即还原。
单篇样式的键是文件路径，重命名 / 移动 / 删除笔记时会自动迁移或清理。

## 性能

导图的每一环都是按「1000 个节点」设计的：文本测量走离屏 canvas（绝不逐节点读
`offsetWidth`）、节点 DOM 只认领与归还不重建、所有连线合并成一条 `<path>`、
平移缩放只改一次容器 `transform`、事件全部委托到图层上（整个视图 17 个 DOM 监听器，与节点数无关）。

仓库里带了一份 1000 节点的压力测试笔记（`bench/1000节点压力测试笔记.md`，
1000 个节点铺满 8 层：6 层标题 + 2 层列表，含长标题、行内标记、正文与代码块），
由 `node scripts/gen-stress-note.mjs` 确定性生成。

`npm test` 会在这份语料上跑一遍纯计算的部分（实测值，Windows 11 / Node 22）：

| 环节 | 1000 个节点 |
| --- | --- |
| 解析（`parse`） | ≈ 0.6ms |
| 对账（`reconcile`，保住 id 与折叠态） | ≈ 0.3ms |
| 布局（三种方向各一次） | ≈ 0.2–0.3ms |
| 打开一篇笔记的全流程 | ≈ 0.6–1.3ms |
| 生成 1000 条连线（直线 / 斜线 / 折线各一遍） | ≈ 0.2–0.3ms |
| 连按 10 次回车（每次都基于上一次的文本） | 每次 ≈ 2ms |

DOM 那一段只有在真实 Obsidian 里才量得准：把压力笔记打开成导图，
再执行命令面板里的**「导图性能自检」**，它会报出布局、首次渲染（重建全部 DOM）
与重绘各花了多少毫秒。

## 已知限制

- 只做**纯文字**导图：不做概要、自由连线、图片、备注、公式。
- 折叠状态不持久化，切换笔记即重置。
- 任务列表 `- [ ]` 按普通文本处理；有序列表读得进来，写回时统一转成 `-`。
- 多选（`Ctrl/⌘ + 左键`）只用于批量删除，不支持批量拖拽移动。
- 上面这些都是有意推迟到 v2 的，不是遗漏。

## 开发

```bash
npm run dev        # 监听构建
npm run typecheck  # 严格模式类型检查
npm test           # 460 项单测
```

目录分层是硬约束：`core/`（解析、序列化、结构操作）与 `layout/`（布局算法）
**不允许 import 任何 obsidian API**，它们是纯函数、有单测；
只有 `doc/DocumentBridge.ts` 碰文件读写，只有 `view/` 碰 DOM。

## 许可证

MIT，见 [LICENSE](LICENSE)。
