# 大纲思维导图（Outline Mindmap）Obsidian 插件

> 极简直观的大纲思维导图：只用最纯净的 Markdown 语法，笔记文件零多余代码；点一下就能定位，导图与笔记双向实时同步。

## 特色功能

- **严格 Markdown 语法，笔记零污染**：标题和列表自动变成导图，不写 YAML、不插属性、不加隐藏注释。你的 Markdown 永远是 Markdown，拿到任何地方都干净可用。
- **优雅动画（Pro）**：展开收起如行云流水——Apple 风格的顺滑缓动，操作哪个节点，它就自动滑到视野中心；大树展开也不会缩成一团看不清，远距离慢慢移、近距离跟手；速度可调，还带微弹性收尾，任何操作都能随时打断。
- **双向实时同步**：改笔记，导图跟着变；拖导图，笔记同步改。一次写回、一步撤销，两边永远一致。
- **点击即定位**：单击节点，自动跳到 Markdown 对应行并高亮；双击就能改文字，大纲和正文之间无缝切换。
- **拖拽即重排**：拖动节点自动换层级、自动改类型，有序列表自动重编号；拖到空白处，直接就变成新的根节点。
- **快捷键闭环**：Tab 加子节点、Enter 加同级、Space 折叠、Delete 删除，全程不用碰鼠标；中文输入法选词时也不会误触。
- **多选批量**：Ctrl 点选或框选，批量删除、批量移动，一次把大纲收拾利索。
- **多样导图样式（Pro）**：8 种布局、4 种连线、多套主题模板和深度自定义，深浅色主题都好看。

## 基本介绍

### 这是什么

大纲思维导图是一款面向桌面端 Obsidian 的插件：它把当前笔记实时渲染成一棵目录式思维导图，并和 Markdown 双向编辑。它不是把笔记"画"成图，而是把 Markdown 里的标题和列表结构直接翻译成导图——H1 是根节点，H2-H6 逐级展开，列表用来表达更深的层级，普通段落等正文则留在笔记里。导图是笔记的"结构视图"，正文和导图永远一一对应。

### 打开方式

- 命令面板（Ctrl+P）→「打开大纲思维导图」：在新标签页打开。
- 命令面板 →「在右侧边栏打开大纲思维导图」：固定到右侧边栏，随时切换显示。
- 视图默认跟随当前活动笔记，切换笔记自动换图；点标题栏的"锁定"按钮，就可以固定住当前笔记。
- 工具栏固定四个按钮：锁定/跟随、单击即跳转、适应画布、设置当前笔记样式。

### 语法与数据规则：纯净到底

- **Markdown 是唯一的数据源**：导图的节点、层级、顺序全部由笔记本身推导；插件只把设置、样式、视图状态存进自己的数据文件，绝不往笔记里写属性、YAML、标签或隐藏注释。
- **哪些内容会成为节点**：H1-H6 标题，以及有序、无序、任务列表（含嵌套）。H1 是根节点，多个 H1 就是多个根；没有 H1 时，最靠前的 H2 及以上作为根。
- **哪些内容不进图**：普通段落、引用、表格、代码块、Callout 等作为"隐藏正文"，挂在最近的结构节点下，原样保留在笔记里。
- **层级规则**：标题级别用 `#` 表达，标题必须顶格，最多 6 级（H1-H6），第 7 层及更深用列表缩进表达；插件新建或改写的列表行统一用 Tab 缩进（一个 Tab 一级），已有的空格缩进行除非被移动或改写，否则保持原样。
- **严格空行（默认开启）**：新建或移动标题时自动预留 3 个空行，方便直接在标题之间写正文；关掉则只补 1 个常规空行。插件触碰的块遵循"标题前空一行、标题后空一行、正文段间空一行"的规范，但不会擅自重排你已有的笔记。
- **任务列表**：`- [ ] / - [x]` 直接成为节点，复选框状态原样保留，写回时完整恢复。
- **前导属性（frontmatter）**：整块忽略，不进导图，也绝不改动。
- **行内 Markdown**：节点文字支持粗体、斜体、删除线、行内代码、标准链接、Obsidian 双链、高亮 `==文字==`、加粗倾斜 `***文字***`，还能互相嵌套；编辑时显示源码，退出后渲染效果，原始 HTML 不允许进入节点。

### 双向实时同步

- **笔记 → 导图**：监听编辑器输入（16ms 防抖），一次变化里的多个文字修改会批量合并，只触发一次轻量刷新；结构变化时先保留旧画面再重建，避免闪屏。
- **导图 → 笔记**：新增、编辑、拖拽、批量操作都会生成标准 Markdown；编辑器打开时一次事务写回等于一步撤销；笔记没打开时直接写文件（没有撤销）。
- **防回环**：插件写回不会触发导图重复刷新，也不会把自己写回的内容再解析一遍，两边状态始终一致。

### 常用操作

- **定位与高亮**：开启"单击即跳转"（默认开启）后，单击节点约 300ms 就会定位到 Markdown 对应行，并高亮该行文字（不含 `#`、`-`、任务框标记）；双击节点直接编辑、不跳转；点击 Markdown 任意位置，高亮立即消失；文章没打开时也会自动打开并定位。
- **编辑**：双击或 F2 进入编辑；编辑中 Enter 确认、Esc 取消、Tab 确认并新增子节点；提交后编辑器光标停在该节点对应行，不跳回文章顶部。
- **新建**：Tab 或节点旁的加号按钮添加子节点，Enter 添加下方同级，Shift+Enter 添加上方同级；双击画布空白处，会在文件末尾追加新的 H1 根节点并立即进入编辑。
- **拖拽**：落点分为"成为子节点 / 上方 / 下方"三种；按落点自动转换类型——列表节点进入根位置或标题层级（新深度不超过 6）时转为对应 H1-H6，标题节点进入列表层级时整棵标题子树转为列表；非法落点显示禁止并回弹；拖到空白处提升为 H1 根节点并移到文末。
- **多选批量**：Ctrl/Command 点击多选，或空白处拖框框选；批量删除、批量移动时，相对顺序、层级、类型、隐藏正文和有序编号一次写回。
- **折叠与画布**：节点可折叠/展开子树；Ctrl+滚轮或 Ctrl+= / Ctrl+- 缩放，Ctrl+0 适应画布，按住 Space 拖动空白平移；折叠状态、缩放和平移按笔记记忆，关闭再打开自动恢复。
- **删除**：删除节点会连同其块内隐藏正文和多余空行一起清理，并在删除点局部归一空行。

### 快捷键总表

| 场景 | 按键 | 行为 |
| --- | --- | --- |
| 未编辑 | Tab | 添加子节点 |
| 未编辑 | Enter | 添加下方同级节点 |
| 未编辑 | Shift+Enter | 添加上方同级节点 |
| 未编辑 | Delete | 删除选中节点及子树（支持多选批量） |
| 未编辑 | F2 / 双击 | 进入节点编辑 |
| 未编辑 | 方向键 | 按布局方向移动选中 |
| 未编辑 | Space | 折叠 / 展开 |
| 未编辑 | Ctrl/Command+左键 | 多选 / 取消多选 |
| 编辑中 | Enter | 确认并退出编辑 |
| 编辑中 | Esc | 取消并还原 |
| 编辑中 | Tab | 确认并新增子节点 |
| 任意 | Ctrl+滚轮 / Ctrl+= / Ctrl+- | 缩放 |
| 任意 | Ctrl+0 | 适应画布 |
| 任意 | 按住 Space 拖空白 | 平移画布 |

### 样式与设置

- **8 种布局**：分支向右（默认）、分支向左、分支两侧、树状/组织结构、时间线、时间线 2、竖向时间线、鱼骨图；切换布局后自动适应画布。
- **4 种连线**：斜向直线、直角折线、圆角直线（默认）、曲线。
- **主题模板**：简洁大纲、经典思维导图、卡片风、时间线等；还可以自定义布局、连线样式、节点间距、节点形状、填充色、边框色、字号。
- **全局与单篇**：全局默认样式 + 单篇笔记覆盖（样式弹窗实时预览，点弹窗之外应用单篇设置）；样式只存插件数据，不写 Markdown。
- **设置页三个分区**：常规（单击即跳转、锁定当前笔记、严格空行）、优雅动画（Pro：开关、速度 0.5x–2x、微弹性收尾）、思维导图样式（Pro）；每个分区都能独立恢复默认。
- **跟随 Obsidian 主题**：深浅色自动适配，节点和编辑框使用 Obsidian 字体。

### 中文输入与性能

- **IME 保护**：拼音/五笔组合期间所有快捷键失效，Enter 确认候选词不会误触"新增节点"，Tab 切换候选不会误触"新增子节点"。
- **性能**：已针对 500 / 1000 / 2000 节点场景压测，上千节点的笔记编辑、缩放、平移依然流畅；节点特别多时建议关掉优雅动画，大图更跟手。

## 使用场景

- **大纲式写作（如 NoteFlow）**：先把大纲列成图，再逐节填正文。NoteFlow 把"大纲即笔记"做成了独立软件；这个插件让 Obsidian 原生获得同样的体验，文件仍是纯 Markdown，不绑定任何专有格式。
- **类脑式笔记（如 Lattics）**：既要大纲层级，也要正文内容。Lattics 主打类脑式知识组织；本插件用最轻的方式复刻了其中"大纲 + 正文"的核心——导图看结构，Markdown 写正文，两不耽误。
- **导图 + 文档一体（如万兴脑图云笔记）**：在导图里组织，在文档里落笔。本插件在 Obsidian 里实现同样的事，但没有账号、没有云端依赖，笔记永远属于本地。
- **读书笔记与知识整理**：一本书拆成多级标题，摘录和想法留在正文，导图一屏看清全书骨架。
- **会议纪要 / 项目规划 / 待办清单**：任务列表直接显示为节点，完成状态一眼可见；用多个 H1 承载"目标 / 进展 / 风险"等板块。
- **长文与多主题笔记**：多个 H1 就是多个根节点，一个文件既能放整本书的章节，也能放互不相干的主题，导图天然就是目录。

## 限制说明

- 所有列表（有序、无序、任务）都会被当作导图节点，包括正文里随手写的序号列表。这是"纯净语法"的刻意设计：需要非节点正文时，请用段落或引用。
- 不支持自由节点的任意摆放：布局始终按 Markdown 结构自动排布，拖拽改变的是结构层级，而不是画布坐标。
- 节点文字内只支持白名单里的行内 Markdown（粗体、斜体、删除线、行内代码、链接、双链、高亮、加粗倾斜等），不支持图片、公式、表格和原始 HTML。
- 标题必须顶格；列表层级依赖缩进，插件新建或改写的列表统一使用 Tab 缩进。
- 插件不会自动整理你已有的笔记，只规范自己新建、移动、转换所触碰的块。
- 笔记没在编辑器里打开时，写回直接改文件，没有撤销记录。
- "单击即跳转"只做定位与高亮，不提供返回历史。
- 首版不含节点复制粘贴、图片节点、摘要节点、关系连线、导出图片/PDF 等功能。
- 仅支持桌面端 Obsidian，不做移动端适配。

## 付费功能（Pro）

### 终身授权

一次购买，单设备终身使用。Pro 功能包括「优雅动画」和「思维导图样式」两项（多样布局、连线样式与主题模板），价格 ¥22，联系我。

### 激活流程

1. 点击购买链接完成购买；
2. 打开 设置 → 第三方插件 → 大纲思维导图 → 激活，查看并一键复制机器码；
3. 按购买链接里的要求，把机器码（和订单号）发给作者；
4. 等待作者人工发放激活码，可能有 1–3 个工作日的延迟；
5. 收到激活码后，在插件激活页粘贴并激活，立刻解锁全部 Pro 功能。

### 隐私与安全

- 机器码由 5 项非敏感硬件参数（操作系统类型与大版本、CPU 架构、CPU 型号、逻辑核心数、内存大小）拼接后，经 SHA-256 单向哈希生成；
- 哈希单向不可逆，原始参数不会离开本机，机器码无法反推出设备信息，也无法定位到具体用户；
- 激活完全离线校验，不依赖网络和服务器；授权绑定设备指纹，卸载重装插件都不影响激活。
