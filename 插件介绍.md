# 大纲思维导图

这一篇本身就是演示品。

它是一篇**普通得不能再普通**的 Markdown 笔记——没有 frontmatter，没有标签，没有任何插件专用语法。
但只要点一下左侧栏的导图图标，它就会变成一张思维导图：你现在读到的这段话在导图里是看不见的，
而上面那行 `# 大纲思维导图` 就是导图的根节点。

> 建议的看法：把这篇笔记打开，再点左侧栏的导图图标，让**笔记与导图左右并排**。
> 然后在导图里点任意一个节点——笔记会滚到对应的那一行并高亮起来。

## 特色功能

1. **严格 Markdown 语法，笔记零污染**：标题和列表自动变成导图，不写 YAML、不插属性、不加隐藏注释。所有设置存在插件自己的 `data.json` 里——你的 Markdown 永远是 Markdown，拿到任何地方都干净可用。
2. **双向实时同步**：改笔记，导图跟着变；拖导图，笔记同步改。写回只替换那一行，范围外逐字节不变；一次操作一步撤销，两边永远一致。
3. **点击即定位**：单击节点，笔记滚到对应行并高亮；双击就能改字。新建、改名、拖拽之后，笔记也停在你刚动过的那个节点上，大纲与正文之间无缝切换。
4. **拖拽重排**：拖到上下缘 = 插到前后，拖到中间 = 成为子节点，拖到空白 = 变成新的根。跨过第 6 层时标题与列表自动互转，底下的正文跟着一起搬家。
5. **快捷键操作**：`Enter` 加兄弟、`Tab` 加孩子、方向键走位、`Delete` 删整棵子树、`Esc` 一律作废不写盘——双手不离键盘。
6. **好看样式**：形状、配色、字号、疏密随手调，连线可选直线 / 斜线 / 折线，布局可向右、向左或两侧均衡分列。全局与单篇两级，拖滑块实时预览，取消即还原。
7. **优雅动画**：节点移动、切换布局、展开折叠都能平滑过渡；默认关着，要不要这份顺滑由你决定。而折叠完，你刚点的那个节点永远在正中间。

## 一句话

把标题大纲画成导图，两边**同一份数据**。

在导图里改一个节点，改的就是笔记里的那一行；在笔记里敲一个字，导图立刻跟着变。
没有导入，没有导出，没有第三份状态需要同步。

### 改导图 = 改笔记

试试看：在导图里选中任意节点，按 `F2` 改几个字，再看笔记——那一行的标题已经变了，
而这一行前后的空行、缩进、标点，一个字节都没动。

### 改笔记 = 改导图

反过来也一样。在笔记里把某个 `##` 改成 `###`，导图里那个分支会当场缩进一层。

### `Ctrl+Z` 一下就退回去

所有写入都走编辑器事务。**一次操作 = 一次撤销**，不会出现「撤销了半步」这种事。

## 怎么读这张图

节点是标题，正文藏起来，代码块不算数。

### 标题 → 前 6 层

`#` 到 `######` 依次是导图的第 1 到第 6 层。

### 列表 → 第 7 层起

标题只有 6 级，再往下就用 `- 列表项`：**一层缩进 = 4 个空格或一个 Tab**，可以一直往下长。

### 正文 → 跟着标题走

标题下面的段落**不出现在导图里**，但它跟着标题一起搬家：
拖动一个节点换位置，它底下的正文会原样跟过去。

### 代码块里的 `#` 不是标题

下面这段里有三个 `#`，但导图里一个节点都不会多出来：

```python
# 这是注释，不是标题
def hello():
    print("#hashtag 也不是标题")  # 行尾注释同理
```

### **粗体**、*斜体*、`代码`、==高亮==

上面这一行是标题，所以它在导图里是一个节点——而且这些行内标记会**照着渲染**，
不会变成一串星号和反引号。[链接](https://obsidian.md)也一样。

## 手上的操作

选中、新建、改名、删除、拖拽，都在图上完成。

### `Enter` 加兄弟

在当前节点后面新增一个同级节点，输入文字，再按 `Enter` 就接着写下一个。

### `Tab` 加孩子

给当前节点新增一个子节点。第 6 层之后按 `Tab`，插件会**自动切成列表语法**。

### 方向键走位

`↑` `↓` 在同级之间走，走到头就退到外层；`→` 进到子节点里，`←` 收起分支或回到父节点。

分支朝左长的时候，「进去」的那个键就是 `←`——方向键按**节点自己朝哪边生长**来解释。

### 拖一下就换位置

拖到节点的上下缘 = 插到它前面 / 后面，拖到中间 = 成为它的孩子，拖到空白处 = 变成新的根。

### `Esc` 一律不写盘

正在改名、正在拖拽，按 `Esc` 全部作废，笔记里一个字都不会写。

## 长什么样由你定

形状、配色、字号、疏密、连线，两级可调。

### 三种分支样式

直线、斜线、==折线==。折线是圆角直角，兄弟们共用一条竖干，像地铁线路图。

### 三种布局方向

向右、向左、两侧。「两侧」会按**子树规模**把根节点的分支均衡分到左右两列。

### 全局与单篇两级

样式分全局与单篇：这一篇没单独设，就跟着全局走。
改名、移动、删除笔记时，单篇样式会自动迁移或清理，不会在配置里留垃圾。

### 拖滑块实时预览

样式窗口开着的时候，拖滑块当场就能看到效果；点「取消」立刻还原，**预览不落盘**。

## 三条不会破的底线

写笔记的人的东西，插件一个字节都不动。

### 绝不写 frontmatter、绝不写标签

所有设置与样式都存在插件自己的 `data.json` 里。你的笔记里不会因为装了这个插件多出任何东西。

### 绝不整篇重写文件

每次写回都是对**特定行范围**的最小替换。范围外的字节逐字节不变——
包括你手写的空行、缩进、行尾空格，以及文件用的是 LF 还是 CRLF。

### 一次操作 = 一次撤销

新建、改名、删除、拖拽，每一样都是编辑器里的一次事务。

## 快不快

按 1000 个节点设计，不是按 100 个。

### 1000 节点的实测

#### 纯计算部分

##### 在这台机器上（Windows 11 / Node 22）

###### 每一项都在毫秒级

- 解析一篇 1000 节点的笔记：约 0.6ms
- 对账（保住节点 id 与折叠状态）：约 0.3ms
- 布局（向右 / 向左 / 两侧各一次）：约 0.2–0.3ms
    - 生成 1000 条连线：约 0.2–0.3ms
    - 连按 10 次回车：每次约 2ms

上面这四层标题 + 两层列表，正好演示了导图能长到第 8 层。

### 为什么快

文本测量走离屏 canvas，绝不逐节点读 `offsetWidth`；节点 DOM 只认领与归还，不重建；
所有连线合并成**一条** `<path>`；平移缩放只改一次容器 `transform`；
事件全部委托到图层上——整个视图 17 个 DOM 监听器，与节点数无关。

### 自己量一遍

仓库里带了一份 1000 节点的压力测试笔记。把它打开成导图，
再执行命令面板里的「导图性能自检」，插件会报出布局、首次渲染与重绘各花了多少毫秒。

## 不做什么

只做纯文字导图，边界画得很清楚。

### 不做的

不做概要、自由连线、图片、备注、公式。这些都是有意推迟的，不是遗漏。

### 暂时的

折叠状态不持久化，切换笔记就重置；多选只用于批量删除，不支持批量拖拽。

### 为什么

每加一种非文字元素，就得在笔记里发明一种插件专用语法——
那样一来，「笔记还是那篇笔记」这条就守不住了。
